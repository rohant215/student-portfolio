import React from 'react';

const Footer = () => {
  return (
    <footer className="footer">
      <p>© {new Date().getFullYear()} Rohan Thosar. Built with React.</p>
    </footer>
  );
};

export default Footer;